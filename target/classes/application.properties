# Configure Spring Boot server port
server.port=5000

# Optional: Disable verbose logging of database initialization
spring.main.banner-mode=off
logging.level.org.springframework.web=INFO

# MySQL Database Configuration
spring.datasource.url=jdbc:mysql://127.0.0.1:3306/ai_career_twin?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC
spring.datasource.username=root
spring.datasource.password=root
